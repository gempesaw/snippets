# -*- mode: snippet -*-
# name: rule
# key: rule
# --
=head2 $1

$2

=cut

if ($string =~ /$1/) {
    # my $$3 = \$1;
    # $status = $browser->$4($$3);
    return(printMessage($original, $status, "XXX", $endOnFail, $browser));
}